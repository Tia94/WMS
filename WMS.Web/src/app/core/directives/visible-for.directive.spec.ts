import { VisibleForDirective } from './visible-for.directive';

describe('VisibleForDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibleForDirective();
    expect(directive).toBeTruthy();
  });
});
